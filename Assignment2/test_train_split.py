"""
This file should contain the split into train and test to only do the transformation on that part.
If we use KFold, this part should contain a callable instead of running it once.
If it is only split once, just create the file, maybe train_extended_df.csv and test_extended_df.csv
"""